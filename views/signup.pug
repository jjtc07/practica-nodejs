extends ./layout.pug
block contenido
  h1.text-center.mt-5 Registro
  form(action="/users" method="POST")
    div.row.justify-content-center.mt-5
      div.col-3
        div.form-group
          label(for="username") Usuario
          input.form-control(type="text", name="username", placeholder="Usuario", id="username")
        div.form-group
          label(for="email") Email
          input.form-control(type="text", name="email", placeholder="Email", id="email")
        div.form-group
          label(for="password") Contraseña
          input.form-control(type="password", name="password", placeholder="Contraseña", id="password")
        div.form-group
          label(for="password_confirmation") Confirmar contraseña
          input.form-control(type="password", name="password_confirmation", placeholder="Confirmar contraseña", id="password_confirmation")
        div.row.align-items-center
          div.col-6
            button.btn.btn-info.w-100(type='submit') Registrarse
          div.col-6.text-right
            a(href='/login') Ya tengo cuenta
    